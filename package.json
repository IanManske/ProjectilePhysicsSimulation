{
  "private": true,
  "source": "public/index.html",
  "alias": {
    "react": "preact/compat",
    "react-dom/test-utils": "preact/test-utils",
    "react-dom": "preact/compat",
    "react/jsx-runtime": "preact/jsx-runtime"
  },
  "scripts": {
    "postinstall": "dotnet tool restore",
    "start": "dotnet fable watch src -s -o fable & parcel",
    "build": "dotnet fable src -s -o fable --run parcel build"
  },
  "dependencies": {
    "preact": "^10.6.6"
  },
  "devDependencies": {
    "@parcel/transformer-sass": "2.5.0",
    "parcel": "^2.5.0"
  }
}
